<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.nbtech.testtiktokvideodownloader">
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"></uses-permission>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.INTERNET" />
     <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:usesCleartextTraffic="true"
            android:theme="@style/AppTheme"
             >

        <meta-data
                android:name="com.google.android.gms.ads.APPLICATION_ID"
                android:value="@string/AdmobappId"/>

        <activity android:name="codestudioapps.tiktokvideo.testtiktokvideodownloader.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                 <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>
        </activity>
        <receiver android:name="com.nbtech.testtiktokvideodownloader.com.tiktokvideo.testtiktokvideodownloader.receiver.Receiver">
            <intent-filter>
                <action android:name="quit_action" />
            </intent-filter>
        </receiver>
        <receiver android:name="com.nbtech.testtiktokvideodownloader.com.tiktokvideo.testtiktokvideodownloader.services.Restarter">
            <intent-filter>
                <action android:name="restartservice" />
            </intent-filter>

        </receiver>

            <service android:name="com.nbtech.testtiktokvideodownloader.com.tiktokvideo.testtiktokvideodownloader.services.ClipboardMonitor"
                      android:stopWithTask="false"
                     android:exported="false"
                     android:label="Clipboard Monitor"

            >

            </service>
    </application>

</manifest>